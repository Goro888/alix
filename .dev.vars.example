# The "Deploy to Cloudflare" button asks for this during setup and saves it as a Secret automatically.
# For local development: copy this file to .dev.vars and paste your key (never commit .dev.vars).
GEMINI_API_KEY=
